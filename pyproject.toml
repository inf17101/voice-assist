[project]
name = "voice-backend"
version = "0.1.0"
description = "Lightweight backend for a WebRTC voice assistant."
readme = "README.md"
requires-python = ">=3.11.7"
dependencies = [
    "fastrtc[vad, stt, tts]",
    "fastapi",
    "openai",
    "uvicorn",
    "numpy",
]
